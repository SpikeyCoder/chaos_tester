name: Deploy to Cloud Run

on:
  push:
    branches: [main]
    paths-ignore: ['frontend/**', '*.md', 'LICENSE']
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  SERVICE: chaos-tester
  REGION: us-central1

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE }} \
            --source . \
            --region ${{ env.REGION }} \
            --allow-unauthenticated \
            --set-env-vars "SUPABASE_SERVICE_KEY=${{ secrets.SUPABASE_SERVICE_KEY }},PERPLEXITY_API_KEY=${{ secrets.PERPLEXITY_API_KEY }},GOOGLE_PLACES_API_KEY=${{ secrets.GOOGLE_PLACES_API_KEY }},GOOGLE_PSI_API_KEY=${{ secrets.GOOGLE_PSI_API_KEY }},WA_SHARED_SECRET=${{ secrets.WA_SHARED_SECRET }}" \
            --quiet

      - name: Append Trello env vars to Cloud Run
        run: |
          gcloud run services update ${{ env.SERVICE }} \
            --region ${{ env.REGION }} \
            --update-env-vars "TRELLO_API_KEY=${{ secrets.TRELLO_API_KEY }},TRELLO_TOKEN=${{ secrets.TRELLO_TOKEN }},TRELLO_LIST_ID=${{ secrets.TRELLO_LIST_ID }}" \
            --quiet

      - name: Set CORS allowed origins
        run: |
          gcloud run services update ${{ env.SERVICE }} \
            --region ${{ env.REGION }} \
            --update-env-vars "CORS_ALLOWED_ORIGINS=https://website-auditor.io,https://spikeycoder.github.io" \
            --quiet
